name: automacao_residencial
description: >
  Módulo Central de Domínio e Lógica de Negócios (Dart puro) para um
  aplicativo móvel de Automação e Monitoramento de Dispositivos
  Residenciais (Tema 09) — Avaliação Processual 1B, Computação Móvel.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
